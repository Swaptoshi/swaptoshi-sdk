'use strict';

const swaptoshiUtils = require('..');
const assert = require('assert').strict;

assert.strictEqual(swaptoshiUtils(), 'Hello from swaptoshiUtils');
console.info('swaptoshiUtils tests passed');
