'use strict';

const liquidPosModule = require('..');
const assert = require('assert').strict;

assert.strictEqual(liquidPosModule(), 'Hello from liquidPosModule');
console.info('liquidPosModule tests passed');
